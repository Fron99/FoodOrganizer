<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.fron99.Foodorganize" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/youtube_profile_image"
        android:label="@string/appName"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodOrganize" >
        <activity
            android:name=".Activities.ActivityCreateFood"
            android:label="@string/title_activity_create_food"
            android:theme="@style/Theme.FoodOrganize.NoActionBar" >
        </activity>
        <!--
        <activity android:name=".Activities.ActivityTotal"/>
        <activity android:name=".Activities.ActivityLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
        <activity android:name=".Activities.ActivityLogin" />
        <activity android:name=".Activities.ActivityTotal" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>